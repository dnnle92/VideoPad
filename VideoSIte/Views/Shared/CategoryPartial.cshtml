@model IEnumerable<VideoSIte.Models.Category>

<ul>
    @foreach (var item in Model)
    {
    <li>@Html.ActionLink(item.CatName, "Index", "Videos", new { category = item.CatId }, null)</li>
    }
</ul>
