@model VideoSIte.ViewModels.VideoIndexData
@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Video Site</h1>
    @*<p class="lead">ASP.NET is a free web framework for building great Web sites and Web applications using HTML, CSS and JavaScript.</p>*@
    <p><a href="#" class="btn btn-primary btn-lg">Learn more &raquo;</a></p>
</div>

@*@Html.Action("CategoryPartial", "Home")

@Html.Action("VideoListPartial", "Home")*@



@*<ul>
    @foreach (var item in Model.Categories)
    {
        <li>@Html.ActionLink(item.CatName, "Index", "Home", new { category = item.CatId }, null)</li>
        <ul>
            <li>
                @foreach (var video in item.Videos)
                {
                    @video.VideoName
                }
            </li>
        </ul>

    }
</ul>*@
